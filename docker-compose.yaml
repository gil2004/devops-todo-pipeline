services:
  todo-api:
    build: .
    image: gilmartins2004/todo-api:v1
    container_name: todo-api-container
    ports:
      - "5000:5000"
    restart: unless-stopped

  # Quando ligarmos a base de dados, o serviço fica assim:
  #
  # db:
  #   image: postgres:16-alpine
  #   container_name: todo-db
  #   environment:
  #     POSTGRES_USER: todo
  #     POSTGRES_PASSWORD: todo
  #     POSTGRES_DB: tododb
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #
  # volumes:
  #   db_data: